{
  "name": "ai-family-tree",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "mobile": "npm --workspace apps/mobile start",
    "api": "npm --workspace apps/api run dev"
  }
}
